// The tab bar floats over tab screens (position: absolute in the tabs
// layout), so screens must reserve bottom space equal to its height.
export const TAB_BAR_HEIGHT = 80;

// Breathing room between the bar's top edge and resting screen content
export const TAB_BAR_GAP = 24;
